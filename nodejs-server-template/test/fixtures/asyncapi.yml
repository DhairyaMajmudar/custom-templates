asyncapi: 2.6.0

info:
  title: Nodejs Backend Server
  version: 1.0.0
  description: This is a Nodejs backend server template

servers:
  dev:
    url: api.example.com
    protocol: http

channels:
  temperature:
    description: default channel
